# install this cron definition on all Vault servers
# backup certs/keys and print new certificates
# 0 0 1,10,20 * * sudo cp -r /etc/certs/*.crt /etc/certs/backup
# 0 0 1,10,20 * * sudo cp -r /etc/certs/*.key /etc/certs/backup
# 2 0 1,10,20 * * sudo /usr/local/bin/consul-template -config=/etc/consul-template.d/your-ct-server-config.hcl -log-level=debug -once