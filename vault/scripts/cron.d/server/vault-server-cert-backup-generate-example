# install this cron definition on all Vault servers
# backup certs/keys and print new certificates
0 0 1,10,20 * * root cp -r /etc/certs/vault*.* /etc/certs/backup
2 0 1,10,20 * * root bash /etc/consul-template.d/certificates/vault/build-vault-server-certs.sh 2>&1 | logger -t vaultservercertgen