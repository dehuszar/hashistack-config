# install this cron definition on all Vault servers
# take vault snapshot, backup snapshot to object store, delete local copy of snapshot
# 0 0 * * SUN vault vault operator raft snapshot save $(date +%F)-$(hostname).snapshot
# 0 1 * * SUN root rclone copy ./$(date +%F)-$(hostname).snapshot Wasabi:<replace-with-your-bucket-name>/vault/snapshots/ -v
# 0 5 * * SUN root rm ./$(date +%F)-$(hostname).snapshot